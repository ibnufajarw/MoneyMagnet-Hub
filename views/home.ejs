<!-- @format -->

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>HOME</title>
        <!-- Bootstrap CSS -->
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
		<style>
			body {
				font-family: 'Arial', sans-serif;
				background-color: #f8f9fa;
				padding: 20px;
			}

			nav {
				background-color: #007bff;
				padding: 10px;
				color: #fff;
				display: flex;
				justify-content: space-between;
			}

			.left a, .right a {
				margin: 0 10px;
				color: #fff;
				text-decoration: none;
			}

			.header {
				margin-top: 20px;
			}

			.header h1 {
				font-size: 36px;
				color: #333;
			}

			.header p {
				font-size: 18px;
				color: #555;
			}

			table {
				margin-top: 20px;
				width: 100%;
				border-collapse: collapse;
			}

			th, td {
				padding: 10px;
				text-align: left;
				border: 1px solid #ddd;
			}

			th {
				background-color: #007bff;
				color: #fff;
			}

			button {
				background-color: #28a745;
				color: #fff;
				border: none;
				padding: 8px 16px;
				cursor: pointer;
			}

			button:hover {
				background-color: #218838;
			}

			.text {
				border: 2px solid;
				margin: 10px;
				padding: 10px;
				border-radius: 4px;

			}
		</style>
	</head>
	<body>
		<nav>
			<div class="left">
				<a href="/" class="text-white">Moneymagnet Hub</a>
				<a href="/add" class="text-white">New stock</a>
				<a href="/financial" class="text-white">Financial</a>
			</div>
			<div class="right">
				<div class="right">
				<a href="/register">Register</a>
				<a href="/login">Login</a>
				<a href="/logout">Logout</a>
			</div>
		</nav>
		<div class="header">
			<h1>Moneymagnet Hub</h1>
			<p class="text">
				Moneymagnet Hub adalah platform keuangan inovatif yang menyediakan
				solusi terintegrasi untuk manajemen portofolio, transaksi, dan
				pemantauan investasi. Dengan desain yang intuitif dan fitur-fitur
				canggih, Moneymagnet Hub membantu pengguna mengoptimalkan keuangan
				mereka dengan mudah.
			</p>
		</div>
		<div>
			<h2>MarketCap</h2>
			<h5>Top Three Stock Price </h5><br>
			<% topThree.forEach(each => { %>
			<h6><%= each.stockAbbrevation %> : Rp. <%= (each.currentPrice * 100).toLocaleString('id-ID') %> / 100 Lembar</h6>
			<% }); %>
		</div>
		<div>
			<table class="table">
				<thead>
					<tr>
						<th>Stocks</th>
						<th>Companies</th>
						<th>Current Price</th>
						<th>Detail</th>
						<th>Action</th>
					</tr>
				</thead>
				<tbody>
					<% stocks.forEach((each) => { %>
					<tr>
						<td><%= each.stockAbbrevation %></td>
						<td><%= each.companyName %></td>
						<td><%= each.currentPrice %></td>
						<td>
							<form action="/stocks/<%= each.id %>" method="get">
								<button type="submit" class="btn btn-success">Show Details</button>
							</form>
						</td>
						<td>
							<a href="/buy" class="text-white">Buy</a>
							<a href="/sell" class="text-white">Sell</a>
						</td>
						</tr>
					<% }); %>
				</tbody>
			</table>
		</div>
		<!-- Bootstrap JS and Popper.js -->
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
	</body>
</html>
